name: Deploy
on:
  push:
    branches:
    - master
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2

      - name: install dependencies
        run: npm install
      - name: run server
        run: node index.js
    
        
